//! lodash's "Methods" group has a single entry, `_.templateSettings.imports._`,
//! a back-reference lodash injects into the template engine. The template
//! engine is not ported (see `template`), and the name is not a valid Rust
//! identifier, so there is nothing to expose here.
